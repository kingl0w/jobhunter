services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./resumes:/app/resumes
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/jobhunter.db
